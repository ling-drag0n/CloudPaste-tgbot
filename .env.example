# 必填：从 https://my.telegram.org/apps 获取
TELEGRAM_API_ID=
TELEGRAM_API_HASH=

# 必填：你的机器人 token（BotFather 获取）
BOT_TOKEN=

# 对外端口：本项目容器对外的 HTTP 端口（默认 7860）
PORT=7860

# 可选：对外前缀（默认 /tg；填 "/" 表示不加前缀）
TELEGRAM_PROXY_PREFIX=/tg

# 可选：bot-api 在容器内部监听端口
TELEGRAM_UPSTREAM_PORT=8081

# 可选：工作目录（配合 volumes 做持久化）
TELEGRAM_WORK_DIR=/var/lib/telegram-bot-api

# 可选：临时目录
TELEGRAM_TEMP_DIR=/tmp/telegram-bot-api-tmp

# 可选：bot-api 详细日志（1~5，越大越吵）
TELEGRAM_VERBOSITY=1

# 可选：网络慢时可以调大
TELEGRAM_DOWNLOAD_WAIT_SECONDS=8
TELEGRAM_DOWNLOAD_POLL_INTERVAL_MS=200
